# Mint Scan v8 — Python dependencies
# Install via: pip install -r requirements.txt (handled by install.sh)

# Core UI
customtkinter>=5.2.0
darkdetect>=0.8.0

# System monitoring
psutil>=5.9.0
pillow>=10.0.0

# Network
requests>=2.31.0
netifaces>=0.11.0
speedtest-cli>=2.1.3

# Optional — system tray (install if desired)
# pystray>=0.19.0

# Optional — desktop notifications cross-platform fallback
# plyer>=2.1.0

# Optional — PDF export for reports
# reportlab>=4.0.0
